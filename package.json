{
  "name": "solana-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "python3 -m unittest discover -s tests -v && node --test cloudflare/scan-dispatcher/test/index.test.mjs dashboard/test/data-source.test.mjs && node --check dashboard/app.js && tsc -p convex/tsconfig.json --noEmit",
    "replay": "python3 tools/replay_reactivation.py",
    "convex:dev": "CONVEX_AGENT_MODE=anonymous convex dev",
    "convex:deploy": "convex deploy",
    "convex:codegen": "convex codegen"
  },
  "dependencies": {
    "convex": "^1.29.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
